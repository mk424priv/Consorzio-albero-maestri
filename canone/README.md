# Canone — Albero Maestri (nuovo prodotto)

Це **workspace канону** нового продукту. Тут живе не код, а **істина продукту**: бачення
досвіду і канонічна специфікація, на яких потім — окремим кроком, після твого «так» —
будується реалізація.

## Що це і чому

Старий застосунок (`/src`, `/supabase`, уся попередня архітектура) вважається **неіснуючим**.
Архітектура стала надто складною і змішаною — ми свідомо її відкидаємо. З чистого аркуша:
новий дизайн, нові дизайн-бібліотеки, нова інфраструктура, новий бекенд, нова база даних.

Єдине, що переноситься зі старого, — **не код, а контекст робочого процесу**: терміни, режими
розрахунку, кодиче парланте, рушій грошей, стани оплат, «Incassa subito», логіка співпраці з
працівниками. Цей доменний контекст видобуто з доменного шару старого проєкту і збережено тут як
канон, бо саме він тримає індивідуальність твоєї роботи.

> **Застарілі специфікації.** Старі файли `docs/CANONE.md` і `docs/PRD-CANONE.md` — це лише
> сирий видобуток домену старого проєкту. Вони **офіційно застарілі** і **не є каноном**:
> їхній словник несумісний із новим (`Collaboratore` замість кореня `Operatore`; немає
> `giornate`/`totale`; «Центр» замість «Soldi»). Канон — виключно ці два документи; де
> старі файли суперечать — праві ці. Повний словник новий ↔ старий — у глосарії Документа 2 (§10.3).

## Документи

| # | Документ | Про що |
|---|---|---|
| 1 | [01 — Концепція бачення користувацького досвіду](01-visione-esperienza-utente.md) | Відчуття, настрій, дизайн-концепція, індивідуальність кожного екрана, дизайн-бібліотеки, прохід усього робочого процесу очима власника. |
| 2 | [02 — Детальна канонічна специфікація](02-specifica-canonica.md) | Архітектура, інфраструктура, ядро, усі сутності, режими, функції, сценарії, під-сценарії й стани. Канон, що закриває кожне відкрите питання. |
| 3 | [03 — План реалізації](03-piano-di-realizzazione.md) | Поетапна дорожня карта: порядок етапів (Tappe 0–11), що входить у кожен, критерій приймання (DoD), розкладка репозиторію. Канон рівня виконання. |

**Порядок читання:** спершу Документ 1 (щоб відчути продукт), потім Документ 2 (щоб побачити, як
відчуття лягає в структуру), потім Документ 3 (щоб побачити, як це будується по етапах). Кожен
наступний спирається на попередній.

## Конвенції цих документів

- **Мова оповіді — українська** (щоб ти читав і затверджував рідною).
- **Мова продукту — італійська.** Кожен канонічний термін, назва сутності, ярлик інтерфейсу,
  значення енумерації наводяться італійською в `моноширинному` написанні або «в лапках» — бо саме
  ці рядки потім стають реальним інтерфейсом. Наприкінці Документа 2 — глосарій UA↔IT.
- **Рішення, а не опитування.** Усе, що ти не розкрив, я додумав і зафіксував як канонічне
  рішення. Де щось свідомо винесено за рамки v1 — це позначено явно.
- **Текстові макети (wireframes)** подані рамками — це схема композиції та ієрархії, не піксельний
  дизайн.

## Статус

🟢 **Реалізація v1 завершена** (Tappe 0–11). Новий продукт побудовано по етапах `03` на гілці
`canone/nuovo-prodotto` — кожен етап окремим комітом, build/тести зелені, кожен екран
верифіковано наживо (скріншоти). Stack: Vite+React19+TS, Tailwind v4, **Dexie (local-first)**,
PWA офлайн. Ядро грошей покрите unit-тестами (`vitest`).

Свідомі рефайнменти поза v1 (працює спрощено, поглиблюється пізніше): підказка «quante ore?»
при конверсії плану; співпраця в режимі `preventivo`; період-фільтр Dashboard; екран `Records`
(§9.5); `storno`/переплата; меню `⋯` (Modifica/Archivia) на картках; PNG-іконки PWA.
