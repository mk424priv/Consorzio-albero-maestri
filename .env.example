# Sviluppo locale (Postgres via docker compose)
DATABASE_URL="postgresql://albero:albero@localhost:5432/albero?schema=public"

# Produzione (Vercel): usa la URL del tuo Postgres (Neon / Vercel Postgres)
# DATABASE_URL="postgresql://USER:PASS@HOST/DB?sslmode=require"

# Password unica di accesso del titolare
APP_PASSWORD="cambia-questa-password"
